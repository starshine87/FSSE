#include "util.hpp"

using namespace std;

uint64_t Util::f2(uint64_t k_s, int id)
{
    
    unsigned char hash[lambda];
    SHA256_CTX sha256;
    SHA256_Init(&sha256);
    SHA256_Update(&sha256, )
}